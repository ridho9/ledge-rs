[package]
name = "engine"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0.100"
ledger_messages = { path = "../ledger_messages" }
rdkafka = { version = "0.38.0", features = ["cmake-build"] }
tokio = { version = "1.48.0", features = ["macros", "rt-multi-thread"] }
zmq = "0.10.0"
