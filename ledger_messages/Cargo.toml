[package]
name = "ledger_messages"
version = "0.1.0"
authors = ["sbetool"]
description = "Example base schema which can be extended."
edition = "2021"

[lib]
name = "ledger_messages"
path = "src/lib.rs"

[dev-dependencies]
criterion = { version = "0.7.0", features = ["html_reports"] }

[[bench]]
name = "sbe_benchmark"
harness = false
